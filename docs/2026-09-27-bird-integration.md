# BIRD Mini-Dev 接入与 20 题试跑记录

日期：2026-09-27。范围为 F 批适配收尾及固定 20 题试跑。模型配置取自当前
`llm.py`（`deepseek-flash`）；运行显式允许不限费用。所有请求仍受每题
14 次模型调用和 120 秒时限约束。未运行 500 题或业务九题付费回归。

## 评测目录与实现

评测适配、官方固定输入、数据库和运行证据统一放在 `benchmark/`。本机原有
Mini-Dev 数据从项目根目录的 `minidev/` 移至
`benchmark/data/minidev/`，文件内容未改，目录整体约 3.3 GB。官方题目与评分
源码缓存位于 `benchmark/data/bird-mini-dev/`。数据及运行目录均由
`.gitignore` 排除，不会随代码提交。

模型仅收到 question 与 evidence。Oracle 单独存放，独立评分时才读取。
Agent 使用既有 NL2SQL 工具调用、数据源注入和 SQL 安全执行链路。官方评分
使用固定版本 EX，对完整查询 SQL 比较结果；这不证明 200 行产品预览完整。

官方来源：[Mini-Dev 说明](https://github.com/bird-bench/mini_dev)、
[canonical 数据集](https://huggingface.co/datasets/birdsql/bird_mini_dev)。
数据 revision 为 `f65faf4ae3b638c1fa6df1d3370c8d92c8366301`，评分 revision
为 `abd11b6db92a1c9f809b32f7564c7c71b34d67f0`。三个下载输入均按 SHA-256
校验。评分不把失败题从分母移除。

## 固定 20 题首次运行

运行目录：[bird-debug-20260927-01](../benchmark/runs/bird-debug-20260927-01/)。
该目录保存配置与数据哈希、每题事件、模型调用记录、独立会话库、预测文件和
官方评分结果；没有覆盖已有运行。

| 指标 | 结果 |
|---|---:|
| 范围 | 固定 20 题，覆盖 11 个数据库 |
| 终态完成 | 1/20 |
| 失败 | 19/20：18 个 `JSONDecodeError`，1 个 `RuntimeError` |
| 官方 EX | 0/20 |
| 模型调用 | 67 |
| 工具调用 | 28 |
| Provider usage | 132,664 tokens（prompt 122,352；completion 10,312） |
| 旧版预留计数 | 709,295（当时按 UTF-8 字节、固定开销与最大输出累计，不是 token 或供应商账单） |
| 累计单题耗时 | 78.3 秒 |
| 已提交压缩 | 0 个会话 |
| 费用 | 未配置单价，金额未知；本次不限费用 |

完成的一题由官方 EX 判错，故总体 EX 为 0。轨迹显示模型响应有时在动作 JSON
前附带说明，或在一次响应中组合多个动作/服务端标记；整段 JSON 解析导致失败。
这是模型输出协议与动作解析入口之间的兼容问题，并非本轮 SQL 护栏拒绝 SQL 的
主要表现。本轮原始请求与响应留存在运行日志中，供后续复核。

我增加了一个动作提取器：从响应中按顺序寻找首个通过 Pydantic 动作校验的 JSON
对象，完整原始响应仍由模型调用日志保存。随后按用户要求，以修复后代码重跑了
相同 20 题。

## 修复后 20 题复跑

运行目录：[bird-debug-20260927-02](../benchmark/runs/bird-debug-20260927-02/)。

| 指标 | 结果 |
|---|---:|
| 终态完成 | 2/20 |
| 失败 | 18/20：13 个 `JSONDecodeError`，5 个 `RuntimeError` |
| 官方 EX | 0/20 |
| 模型调用 / 工具调用 | 79 / 41 |
| SQL 执行次数 | 10 |
| Provider usage | 210,858 tokens（prompt 193,863；completion 16,995） |
| 累计单题耗时 | 115.12 秒 |
| 已提交压缩 | 0 个会话 |
| 费用 | 未配置单价，金额未知；本次不限费用 |

协议解析改进后，完成题数从 1 增至 2，但没有正确 SQL。剩余轨迹仍有 DSML
标记格式，动作提取器不会把该格式转换为动作；另有 5 个流异常终态。模型调用
日志只留异常类型，没有保存 provider 的 finish_reason，无法从既有记录进一步
区分具体的流中断原因。工具链中有 4 次 SQL 安全拒绝，不能把所有失败归为解析。

## 验证与门禁

第二次试跑时，本地测试共 115 项通过；Benchmark 与相关核心模块 Ruff 检查通过，增量 mypy
检查通过。修复后的运行也使用统一 token 估算器和每请求余量。运行摘要保留了
旧的 accounting 说明文字，之后已修正该字段；这只影响描述，不影响预留数值。
官方评分器完成了两次 20 题评分，均为 0/20。第二轮数据库哈希与运行清单相符；
SQLite 以只读模式打开。
第二轮之后又修正了预算说明字段，并加入 0 EX 门禁；这两处不改变已评分 SQL，
但工作区代码指纹已不同于第二轮清单。后续 500 题还需在解决动作协议/流异常后，
用当时冻结的代码配置重新完成 20 题。

第二次运行结束时，500 题尚未通过门禁：修复后 20 题为 0 EX，配置门禁要求固定 20 题至少有一个官方
精确匹配，并且模型、代码、语义目录、数据库及运行参数与冻结清单一致。当前
主要后续工作是让 DeepSeek 动作输出稳定符合协议，并提高实际 SQL 正确率，再
验证固定 20 题。业务九题的正常/压缩双轨脚本已迁入 benchmark，但本次未运行，
也不能从 BIRD 单轮 EX 推断多轮压缩保真。

从项目根目录的运行方式和目录说明见
[`benchmark/README.md`](../benchmark/README.md)。历史计划保留在
[`2026-09-26-post-l29-repair-plan.md`](2026-09-26-post-l29-repair-plan.md)；
本次状态以此记录为准。

## 协议与护栏修复（第三次试跑）

本批仅修复动作输入适配、供应商结束原因与日志、SQL 逻辑运算符误拒绝及提示。
不引入 DSL、工具继承体系或原生 function-calling 迁移；不降低 decision 的输出
上限，不扩大评测范围。沿用 minimal-diff 原则，将动作模型和解析集中到
`model_actions.py`，其余沿用现有调用链。

- DSML 支持直接工具参数、`args` 包装和旧轨迹实证出现的 `ToolAction` 包装。
  每步只接受一个完整调用，拒绝冲突、重复参数及未知工具；仍经过现有参数校验
  和 SQL 护栏。原始输出完整记录，缺少 thought 时只生成中性的调用说明。
- `length` 不再混同断流。决策可从截断响应恢复完整合法动作，不完整动作仍只
  允许原有的一次格式修复。最终回答保留部分文本，但轮次为 failed、错误为
  `OutputTruncated`，不当作成功答案或送评预测。模型调用本身记录
  `status=truncated`、`finish_reason=length`；摘要截断也不得提交。
- `model_calls.finish_reason` 采用幂等增量迁移，历史未知原因保留 NULL，
  不回填推断。正常 stop、缺少结束标记、其他结束原因及真实传输异常分别留证。
- SQLGlot 把 AND/OR 也归入 Func，现仅豁免这两个逻辑节点的函数名检查；
  子节点函数、表列白名单和只读边界仍检查。提示新增禁止 DSML、Markdown、
  额外说明和模拟工具结果的约束，兼容解析作为后备而非目标协议。

离线共 142 项测试通过；相关核心模块与 benchmark 适配源码的 Ruff、增量 mypy
通过。验证包括截断决策恢复、最终部分回答留档且唯一失败终态、摘要截断拒绝、
未知结束原因拒绝、工具参数与嵌套危险函数校验、旧表增量迁移。对第二次运行的
76 次原始决策响应只读回放，均能提取结构合法动作；这不是 76 次业务正确性证明。
检查不扫描外部官方缓存源码的类型问题，也未改动该缓存。

### 单次真实复跑结果

运行目录：[bird-debug-20260927-03](../benchmark/runs/bird-debug-20260927-03/)。
与第二次运行使用相同 20 个题号、业务数据哈希、`deepseek-flash`、1024 输出上限、
每题最多 14 次模型调用及 120 秒时限。代码在运行前冻结，结束后核对指纹未变化。
只运行一次，不根据本次 gold 或得分继续调优、补跑。工程测试与真实模型结果分开报告。

| 指标 | 第二次运行 | 本次运行 |
|---|---:|---:|
| 完整完成 | 2/20 | 10/20 |
| 官方完整 SQL EX | 0/20 | 4/20（20%） |
| 失败 | 13 JSONDecodeError + 5 RuntimeError | 9 OutputTruncated + 1 JSONDecodeError |
| 模型调用 / 工具调用 | 79 / 41 | 138 / 96 |
| execute_sql 工具尝试 / 缓存复用 | 10 / 0 | 32 / 0 |
| Provider prompt / completion tokens | 193,863 / 16,995 | 629,676 / 39,934 |
| Provider total tokens | 210,858 | 669,610 |
| 累计单题耗时 | 115.12 秒 | 248.43 秒 |
| 已提交压缩 | 0 | 0 |

本次 20 题均至少成功执行过一次查询，共留存 30 份查询结果。19 题到达最终回答
阶段，但只有 10 题完整结束。官方 EX 正确题号为 1317、719、5、89；其余失败仍
在 20 题分母中。没有因 SQL 已执行就把截断回答计作成功，也未从自然语言补造预测。

138 次模型调用均有 usage 和结束原因：决策 115 次 stop、4 次 length；最终回答
10 次 stop、9 次 length。两个 length 决策中的完整动作被恢复；另两个来自题 92，
内容均为空，耗尽一次格式修复后终态为 JSONDecodeError。该题的底层原因实际是
输出预算耗尽，可由对应模型日志追溯，不应再归为 DSML 不兼容。

9 次最终截断中，8 次可见文本为空；其中 7 次的 reasoning_tokens 已占满 1024，
另一次为 1022。余下一次只得到 141 字符、reasoning_tokens 为 942。以上仅采用
供应商 usage 计数，不推断或复述内部思维链。因此“输出预算太大鼓励解释”不足以
解释本轮主要失败；直接降低至 512 没有实证支持。2 次缺 thought 的 JSON 动作
仍触发格式修复，均成功恢复；兼容层没有擅自补齐不满足 JSON 契约的动作。

本次实际发送的 ContextView 序列化大小累计 2,477,363 bytes，单次最大
187,305 bytes；对应应用估算累计 628,071 tokens、单次最大 46,875。
供应商 usage 与应用估算分别记录，不混算。预算预留为 1,052,007 tokens，
不是实际用量或账单；未配置单价，费用金额未知。未触发压缩，不能据此宣称
多轮压缩保真通过。

### 剩余问题与边界

1. **输出预算仍是主要阻塞。** 下一批应分阶段处理最终回答的预算与长度要求，
   先验证当前供应商配置，再做有界实验；不能仅把 length 改成 completed 来提高完成率。
   连续空决策的终态仍是通用 JSONDecodeError，后续可在保留原始结束原因的基础上
   改善面向用户的错误分类，避免排障误读。
2. **查询成功不代表答案 SQL 符合题意。** 6 个已完成但 EX 错误的查询含额外列，
   其中部分还返回了多余候选行。例如题 1472 用 LIMIT 5 回答“最少者”，题 1312
   在专业名称外还返回姓名和关联键。应区分探查查询与最终答案查询，不能修改官方
   评分器忽略列或只取第一行来刷分。
3. **还有一个方言映射缺口。** `INSTR` 经 SQLGlot 转成 `STR_POSITION` 后仍被
   函数白名单误拒绝（题 847）。本轮 AND/OR 误拒绝未再出现，但不能据此说护栏已
   覆盖所有函数别名。本轮另外两个工具错误分别是错误参数名和 GROUP_CONCAT
   单元格超过资源限制；后者应保留资源护栏，让 Agent 缩小探查，不直接放宽限制。

所有本次日志写入运行目录的 `sessions.db`：20 条轮次终态、20 个 done、138 次
模型调用、96 对工具调用与结果事件；包含实际输入、原始可见输出、结束原因、
usage、SQL 与结果。根目录 `sessions.db` 和 `business.db` 前后 SHA-256 相同，
旧两次运行未迁移或覆盖。评分器验证了数据库哈希未变。

本批修复及约定的单次 20 题复跑完成。4/20 已满足非零 EX 的最低门禁条件，
但不是产品正确性验收；500 题和业务九题真实模型回归均未执行。任何后续核心
代码或模型配置变更，仍需新的固定 20 题记录，不能沿用本次代码指纹。
