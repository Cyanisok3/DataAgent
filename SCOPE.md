# Data Engineering Agent · 第一阶段范围

更新：2026-09-11。状态：方案已确定，尚未实现或开展模型评测。

## 目标

面向 dbt repository，将自然语言工程需求转化为可检查的 SQL/YAML 修改，执行 dbt 验证，并通过 Spider 2.0-DBT 官方 evaluator 检查任务结果。

第一阶段回答两个问题：最小闭环能否运行；哪些失败值得下一阶段投入。暂不证明整个业务领域的市场需求，也不把小样本结果当作生产可靠性或完整榜单成绩。

## 本阶段做什么

- 使用 Pi Coding Agent SDK + TypeScript，复用 repo 读写、工具循环和会话能力。
- Agent 读取任务原始 repo、schema、可观察的 dbt 依赖，修改 SQL/YAML，执行 `dbt build/test`，根据可见错误在运行限制内修复。
- Python 承担必要的 dbt/DuckDB 调用和官方评测适配；先用本地进程，不拆服务。
- 输出文本 diff、工具执行记录、dbt 验证证据、官方判定及简短 execution receipt。
- 使用 **轻量 3 题 + 常规 3 题 + 较难 3 题，每题独立运行 2 次，共 9 题、18 runs** 的 diagnostic set。

分层和运行规则统一见 [EXPERIMENT.md](EXPERIMENT.md)。三层表示预先观察到的相对结构复杂度，不代表已经验证的模型难度。

## 暂不做什么

不做传统 NL2SQL/BI 产品、前端平台、自建 benchmark 或评分器。不 fork MaloneTalk/DataAgent 或 SignalPilot；它们仅供思路参考。

暂不增加 semantic layer/contract、独立语义 verifier、专用 DAG 检索系统、跨任务 memory、durable execution、多模型 routing、多 Agent、AgentScope Java 或原有 C++ 基础设施。读取现有依赖信息不等于需要搭建新的 DAG 平台。

不设置或记录费用上限，不做费用核算模块；不记录任何哈希值信息。时间与执行步数限制用于终止失控运行，具体值只在实验文档维护。

## 完成标准

1. 一个入口能够完成“任务准备 → Agent 读改 repo → dbt 验证/有限修复 → 收集提交产物 → 官方评分”，并能从原始输入重新启动。
2. 固定的 18 runs 均有记录；成功、任务失败、超时、环境或评分故障分别说明。没有官方判定的运行保留为未评分，不静默丢弃，也不据此宣布评测完成。
3. 分别报告 dbt 验证状态和官方判定，附各层结果、每题两次结果及少量失败证据。**不要求达到预设通过率，也不要求全部成功。**
4. 形成一页内的诊断结论：选择一个有重复证据的失败机制继续改进，或明确当前证据不足、先停止扩展。

若环境或 evaluator 故障在一次集中修复后仍阻塞，交付阻塞原因并停止扩样；此时完成的是诊断记录，不能声称官方评测已完成。

## 后续模块的进入条件

同类失败至少在两个不同任务中出现，且日志支持它与拟增加模块的因果联系，再讨论一个最小改进。同一题两次失败主要说明可复现性，不能单独证明问题广泛存在。

先排除环境、工具适配和停止条件问题。只做一个有依据的机制及对应比较；没有明显失败聚类时，不为了简历补齐架构。下一轮改进实验单独规划，不自动扩大本轮 18 runs。
