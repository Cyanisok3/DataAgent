# DataAgent 技术反思

更新：2026-09-13。只记录影响下一步选择的经验；执行事项见 [REVIEW.md](REVIEW.md)。三层按故障归属划分，不是必须依次做完的架构清单。当前主要在第二层。

## 1. 评测基础设施：让实验有效、结果可信

- **可提交、可评分与做对题分开。** 本轮 12/12 有效提交、3/12 通过；修复产物收集或评估阻塞，只能证明减少了漏评，不能算推理能力提升。
- **轻量评测用于找失败模式。** 固定 12 题、每题一次可支持诊断，不能证明稳定提升；工具、提示词、并行度同时变化时尤其不能归因。先用已有产物验证确定性修复，再决定是否值得花一次模型运行。
- **统计必须落到真实动作。** 220 条探查开始/结束事件实际是 110 次调用；调用、成功、产出和最终得分分别计数。基线已有表不算 Agent 成果，失败写入不算成功修改。
- **官方 evaluator 保持独立。** gold 差异可用于事后审计，不能反馈给 Agent 修复或充当其需求清单。分数不一致先查提交副本和原始证据，避免把错误归因写成新需求。

## 2. 基础操作与观察：让模型能行动、能看到后果

- **工具存在不等于观察有效。** inspect_database 已覆盖 12 题，但 29/110 次因缺参数失败；应先澄清调用契约。结果已进入上下文，不能仅凭失败就推导出需要 memory 或强制摘要。
- **受限工具要提供可用的反馈。** synthea 的真实错误在日志尾部，16,000 字符的头部截断让 Agent 看不到它。补齐错误返回属于基础观察能力，不是新的诊断算法；修复后系统成功率仍可能改善。
- **参考官方的闭环，也核对它的补救入口。** 本地官方框架将执行输出作为下一轮 Observation；它同样只保留前 30,000 字符，但提供 Bash，允许模型另查日志。DataAgent 应在现有 dbt 工具中保留错误及汇总，不能照搬截断再给一个不可读的日志路径。[执行与截断](../spider2-framework/methods/spider-agent-dbt/spider_agent/envs/spider_agent.py) · [观察进入模型](../spider2-framework/methods/spider-agent-dbt/spider_agent/agent/agents.py)
- **基础补齐也要有停止条件。** 文件能成功修改、库结构可读、关键执行错误可见后，就应验证具体任务策略，不继续为完整架构增加模块。基础接口与策略优化的边界并不绝对，区别在于修复信息缺失，还是改善如何利用信息。

## 3. 能力优化：让任务理解、行动选择与验证更有效

- **需求理解先利用可见证据。** nba 的目标模型已在原始 YAML 中；先核对自然语言需求、已有模型说明与待实现产物，不从 gold 读取目标表。候选产物是简短的任务清单，不是先建通用 semantic layer。
- **依赖定位先区分输入缺口与代码缺陷。** tpch 基线仅有 customer，zuora 缺 dbt 包；先核查官方输入与环境准备，不能一律归因于模型不会写 SQL，也不能靠删依赖、删测试换取构建成功。
- **修复策略需要改变失败条件。** 同一错误且无相关修改时反复构建价值很低；修改后重复同参数构建却是必要验证。下一项可验证的策略是中期零成功业务修改时提醒一次，要求最小实现或具体阻塞，避免只追求写入次数。
- **语义验证从一个失败模式开始。** superstore 的列值集合相同不代表逐行关联正确，代理 ID 变化也不等于关联错误；可研究基于业务键的关联与粒度检查。divvy 的“只差 1 行”经审计不成立，提醒我们先证实根因，再设计规则。优先验证一个不依赖 gold 的机制，而不是堆叠 verifier、memory 或多 Agent。
