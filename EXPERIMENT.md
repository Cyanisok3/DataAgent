# Spider 2.0-DBT · 小规模诊断协议

更新：2026-09-11。状态：协议已确定；任务 ID、模型配置、依赖版本待接入时填写；尚未运行。

## 目的与规模

固定单模型、单套 Agent 配置，运行 **轻量 3 题 + 常规 3 题 + 较难 3 题，每题 2 次，共 18 runs**。使用官方任务与官方 evaluator；诊断失败和运行稳定性，不自造 benchmark，不据此估计全量榜单能力。

## 任务选择与预分层

只读取官方任务描述、原始 repo、schema 和静态可见依赖；不读取 gold、参考实现、其他 Agent 的解题轨迹，也不使用本系统运行后的成败或最终 diff 分层。[官方任务清单](https://github.com/xlang-ai/Spider2/blob/main/spider2-dbt/examples/spider2-dbt.jsonl)

先限定为可在本阶段本地 dbt/DuckDB 环境运行的任务，记录被排除的环境条件。为控制选题成本，从按任务 ID 排序的候选清单中，用 Python `random.Random(20260911)` 无放回抽取至多 18 题作为待检查池；只做一轮轻量静态检查，选题最多投入约 2 小时，不搭建复杂度分析平台。

记录任务相关文件数估计、涉及 model 数、可见最长依赖链边数及一句分层理由；文件数指预计相关范围，不是整个 repo 文件总数或解答修改文件数。以下三项各记 0/1/2，作为粗略排序依据：

| 观察项 | 0：较少 | 1：中等 | 2：较多 |
| --- | --- | --- | --- |
| 修改范围 | 单 model，相关文件约 1–2 个 | 约 2–3 个 models 或 3–5 个相关文件 | 更多 models/文件，或需协同修改宏、配置等 |
| 依赖结构 | 可见链长 0–1 条边，无分支协调 | 可见链长 2–3 条边，或需协调多个上游分支 | 更长链路，或多阶段分支合并 |
| 逻辑约束 | 单一输出和直接映射/聚合 | 多项明确约束、多个输出或一般窗口计算 | 跨 model 口径一致性、时序状态或多个相互依赖的约束 |

同一项触发多个档位时取较高档。描述字数仅作辅助，不能把“描述长”等同于“难”。静态依赖缺失或动态生成时标注 `unknown`；若不能通过简短检查澄清，保留未知项并移出本轮可分层池，不伪装成低复杂度。

按总分升序、同分按任务 ID 排序，将可分层池分为人数尽量相等的低、中、高三段，再用同一固定随机种子从每段各抽 3 题。若不足 9 题或三段缺少可辨别的结构差异，记录限制，在模型运行前调整候选范围；不能为凑齐数量宣称已经形成可靠难度层次。

这是**候选池内的相对结构分层**，三段切点和每题理由一起保存，不是经成功率验证的难度标签。已有公开任务级成功率仅在来源、模型和运行条件清楚时作附注，不为寻找它额外开展调研，也不根据它追选高分题。

运行前保存候选清单、排除理由及最终 9 题清单，字段为：`instance_id / project / scope_observations / dependency_observations / constraint_observations / tier / selection_reason`。当前尚未选出具体任务。

## 固定配置与停止条件

| 配置 | 本轮规则 |
| --- | --- |
| 模型 | 单一 provider、准确 model ID；尚未选定。固定 thinking、temperature 等实际支持的参数，未支持的标明 N/A |
| Agent | 同一份 system prompt、工具配置、SDK 设置；本协议关闭自动 SDK 压缩以便准确计数模型请求，不增加专用 memory |
| 环境 | 记录 Pi、Node、Python、dbt、DuckDB 的版本名称；官方材料记录来源 URL、取得日期并保留本轮使用副本 |
| 单 run 限制 | 初始默认墙钟时间 15 分钟、最多 30 次模型请求尝试，任一先达到即停止；自动重试也计入 |
| 命令限制 | 单次工具命令默认 5 分钟，且不得超过 run 剩余时间；超时终止对应进程组及其后代 |
| 评分限制 | 外部 evaluator 每次调用默认 10 分钟，超时记评分故障，不修改官方判分逻辑 |
| 费用与哈希 | 不设置或记录费用上限，不做费用核算；不计算、采集或保存任何哈希值信息 |

以上超时是起始工程默认值，不是官方 benchmark 规定。环境准备不计入 Agent 时间；Agent 开始后，模型等待、工具执行和修复均计入。模型与依赖具体值需在正式运行前填写，18 runs 期间保持不变。

## 两次运行与官方评分

先做不调用模型的环境及提交格式检查，再开始固定配置下的诊断。两轮各运行相同 9 题，每题均从原始 repo、全新数据库副本和新会话开始；不继承上一轮修改、记忆或输出。两轮之间不调整 prompt、工具或模型，也不把第一轮评分反馈给第二轮。

官方 evaluator 将提交记录按 `instance_id` 转成字典，同一份提交清单中重复 ID 会覆盖。因此两轮使用不同 result 目录，分别评分；每轮保留原始官方 ID，不能通过添加后缀制造新的官方任务 ID。[官方 evaluate.py](https://github.com/xlang-ai/Spider2/blob/main/spider2-dbt/evaluation_suite/evaluate.py)

每个提交目录只放一份 `results_metadata.jsonl` 及提交产物。元数据由适配层生成 `instance_id`、`answer_type`、`answer_or_path`；diff 和 dbt 日志是项目诊断证据，不能代替官方要求的提交结果。其他运行日志放在提交目录之外。

使用官方 `evaluation_suite/evaluate.py` 的 `--result_dir` 与 `--gold_dir` 接口，保持评分逻辑不变。实际输出路径和产物类型在接入时按官方说明核对，尚未声称本地验证成功。

## Gold 隔离与记录

Agent 仅获得任务原始输入、工具执行反馈和任务允许的数据。Agent 触发的 dbt 与最终 dbt 验证只在受限子进程后端中访问复制的 task repo 和 run runtime；gold、参考解、评测脚本及评分输出均不在该后端的可读路径中；限制任务工具下载公开答案。官方评分在 run 结束后进行，仅用于外部诊断，不进入修复循环。

每个 run 的 receipt 保留以下最小字段；大文本作为附件引用，不塞入 JSON：

| 类别 | 字段或附件 |
| --- | --- |
| 身份与配置 | `run_id`、官方 `instance_id`、`tier`、`repeat`、固定配置名称及配置全文 |
| 执行 | 开始/结束时间、耗时、模型请求次数、停止原因；模型返回 token 用量时可原样保留用量数字 |
| 修改与工具 | 最终文本 diff、工具调用参数、输出、退出码；保存可见消息，不要求模型提供隐藏思维过程 |
| 验证 | 实际 dbt 命令、检查范围、退出状态、错误摘要；原有检查与 Agent 新增检查分别说明 |
| 判定 | 官方 score/verdict、评分执行状态、产物路径、简短失败标签及对应日志位置 |

项目记录不包含文件/提交/prompt 哈希、checksum 或模型 fingerprint；diff 不带 `index` 哈希行。日志和 SDK 事件只选择上述诊断内容落盘，不直接转储带哈希字段的完整内部对象。组件内部计算不因此重写；其哈希元数据不作为项目记录保存。

官方评分输出只在评测侧保存，不能成为 Agent 的下一次输入。用任务 ID、运行编号、版本名称、配置全文及日期追溯本轮，不建立哈希审计体系。

## 结果解释与结束

保留全部 18 个计划 run 的状态。官方 evaluator 只处理提交与 gold 的 ID 交集，因此缺少提交不能从本项目的诊断汇总中消失；缺失、超时和未评分分别列出，不能凭空补写官方失败分数。

报告每题两次官方结果、各层 6 runs 的结果、总体获官方成功判定数/18，以及已评分数/18；有未评分项时明确称为诊断完成情况，不能冒称完整官方成功率。dbt 通过情况另列。用 0/2、1/2、2/2 描述观察到的一致性，不把 18 runs 当成 18 道独立题，也不报告 best-of-two 为单次通过率。

失败归因先人工轻量查看相关日志，区分环境/工具、定位与依赖、修改错误、构建通过但官方失败、超时等。仅凭“dbt 通过、官方失败”不能直接断定需要 semantic verifier，必须查看具体证据。

完成本轮后停止扩样，不增加第三次尝试来挑选成功结果。后续改进和重跑另开实验记录；本轮被查看过的题不再作为未见测试集。项目完成标准见 [SCOPE.md](SCOPE.md)。
